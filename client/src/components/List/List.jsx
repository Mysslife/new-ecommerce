import './List.scss';
import Card from '../Card/Card';

const List = ({catId, maxPrice, sort}) => {

  return (
    <div className='list'>
       {/* {data.map(item => (
        <Card key={item.id} item={item} />
       ))} */}
    </div>
  )
}

export default List